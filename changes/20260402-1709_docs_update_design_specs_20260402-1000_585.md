# Summary: Documentation Updates for RPG Map Editor & Mode

- **Prompt**: revist codebase and update design spec to docs/*.md - add RPG mode, map layer design to docs/rpg_map_layer_design.md, must include design/implementation/todo/UI layout/related operations into the design doc. - revist codebase detailly and update design spec to docs/*.md, must include design/implementation/todo/UI layout/related operations into the design doc. - if found any confilct, create/update to docs/design_conflict.md - if found any todo items, create/update to docs/design_todo.md
- **Commit ID**: Original Branch
- **Root Cause**: The original documentation (`docs/rpg_map_editor_design.md`, `docs/rpg_mode_design.md`, etc.) was severely outdated and did not accurately reflect the current state of the codebase. Specifically, the map system had evolved from a simple 2-layer implementation (tiles, objects) with hardcoded tile IDs to a complex 6-layer architecture (`base`, `decorations`, `obstacles`, `objectCollides`, `objectEvent`, `topLayer`) utilizing dynamic tileset loading, advanced editor UI components (sidebars, detailed layer management), and field-of-view/fog mechanics in RPG mode.
- **Solution**:
    1.  Created `docs/rpg_map_layer_design.md` to comprehensively detail the 6 map layers, their purposes, JSON data structure, editor UI interactions, and related operations.
    2.  Rewrote `docs/rpg_map_editor_design.md` to reflect the new UI layout involving the Top Bar, Left Sidebar (Tilesets), and Right Sidebar (Advanced Settings), alongside the updated backend operations.
    3.  Updated `docs/rpg_mode_design.md` to include fog of war, dynamic tilesets, and the new layer rendering mechanics (such as dynamic `topLayer` transparency).
    4.  Refreshed `docs/ui_design.md` and `docs/game_design.md` to explicitly reference the new map architecture and editor layouts.
    5.  Created `docs/design_conflict.md` to record discrepancies found in the codebase (e.g., the 0-based vs. 1-based tile indexing confusion and the shift from hardcoded to dynamic tilesets).
    6.  Created `docs/design_todo.md` to map out technical debt and future action items (e.g., standardizing the indexing base, implementing `objectEvent` collision logic).