# Task Summary: Extract RpgMapEditor

**TID:** 550
**CHANGE_PREFIX:** 20260329-0810

## Prompt
Refactor: rpg mode 的相關檔案
- 將rpg mode 的地圖編輯相關檔案 獨立出來, 獨立為: rpg_map_editor.tsx
- 更新rpg mode 設計文件到docs/rpg_mode_design.doc
- 更新地圖編輯的設計文件到docs/rpg_map_editor_design.doc
- 相關的文件要包含設計, UI, 跟前後端實作, 跟外部資料(such as DB, supabase)的連動

## Original Branch Commit ID
N/A (Working on current branch)

## Root Cause
The `RpgMode.tsx` component was too large and contained both the game logic and the map editor logic, making it difficult to maintain and scale. It violated the single responsibility principle.

## Solution
1. **Extracted RpgMapEditor**: Created a new file `src/components/RpgMapEditor.tsx` by moving all the map-editing-specific logic and UI out of `RpgMode.tsx`.
2. **Simplified RpgMode**: Removed the map editor UI and logic from `RpgMode.tsx`, and conditionally rendered the new `<RpgMapEditor />` component when `mode === 'edit'`.
3. **Documentation**: Created `docs/rpg_mode_design.doc` and `docs/rpg_map_editor_design.doc` detailing the design, UI, frontend/backend implementations, and database interactions for both features.
4. **Verification**: Ensured that the application still compiles cleanly without TypeScript errors by running `npm run lint` and `npm run build`.
