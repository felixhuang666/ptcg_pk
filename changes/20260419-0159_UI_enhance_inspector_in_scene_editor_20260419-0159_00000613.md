# Summary

- **TID**: 00000613
- **Prompt**: Objective:[feat] enahnce inspector in scene editor. - when designer click a map or a object in scene editor, it is now showing information of the map or game object. I would like to add "edit map" button for the map in left side bar (inspector) or "edit game object". - when designer click the "edit map" or "edit game object", it should open a new windows and call map editor or game object editor. - check if game object editor is designed and implemented, if not, design and implement it.
- **Original Branch Commit ID**: N/A
- **Root Cause**: The RpgSceneEditor's inspector did not have actionable buttons to dive into detailed editors for maps and game objects. Also, the GameObject editor did not have a dedicated editable view, only a creator prompt dialog.
- **Solution**:
  1. Added "Edit Map" button in RpgSceneEditor which opens `/?view=RPG_MAP_EDITOR&mapId={id}` in a new tab.
  2. Modified `RpgMapEditor.tsx` to initialize its state from the `URLSearchParams` to directly load the map via `mapId`.
  3. Added "Edit Template" button in RpgSceneEditor for game objects.
  4. Modified `App.tsx` to handle a new `GAME_OBJECT_EDITOR` view, and map it via URL params.
  5. Enhanced `GameObjectTemplateCreator.tsx` to act as an editor if `templateId` is supplied in the URL, fetching existing data via API and populating the form.
