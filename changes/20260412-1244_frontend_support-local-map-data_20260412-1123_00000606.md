# Feature: Support local map data
- TID: 606
- Change Prefix: 20260412-1244
- Original branch commit id: Not applicable
- Root cause: Missing support to save individual maps directly to the local filesystem from the Map Editor.
- Solution:
  - Added a "Save to Local Asset" button in `src/components/RpgMapEditor.tsx` next to the main save map button.
  - Implemented the corresponding click handler to hit the `/api/save_local` endpoint passing the map data.
