# Task Summary

**Prompt**:
Objective: in map editor, add position of current mouse's pointer
- in map editor, it now displays the map name and cam in top-left corner, i would to display current grid column/row index (1-base).
- add id and debug msg for this area, so that I may add other data for this area later.
- update UI layout to docs/rpg_map_editor_design.md

**Original Branch Commit ID**: N/A

**Root Cause**:
The `RpgMapEditor.tsx` component's UI overlay historically only displayed the camera coordinates and hardcoded the string "World". There was no dynamic display of the current pointer's grid location, area ID, or debug messages.

**Solution**:
1. Updated `MainScene.update()` in `src/components/RpgMapEditor.tsx`.
2. Grabbed `this.input.activePointer` and translated it to world points using `cam.getWorldPoint(pointer.x, pointer.y)`.
3. Converted world coordinates into 1-based tile grid indexes using `Math.floor(worldPoint / tileSize) + 1`.
4. Appended `Pos: Col {col}, Row {row}`, `ID: `, and `Debug: ` to the `infoTextRef.current.innerText` overlay.
5. Injected `mapName` dynamically via React props so the overlay accurately reflects the actively selected map.
6. Updated `docs/rpg_map_editor_design.md` under "UI Design" to document the updated Info Overlay capabilities.
