# Update TODO List with Missing Design Specs

**TID:** 00000625
**Change Prefix:** 20260424-1701

## Summary
Reviewed the design specifications within the `docs/` directory and found two previously implemented features missing from the `Resolved Items` list in `docs/TODO.md`: "Multi-map RPG Scene Rendering" and "Quest Player Restore". These items were successfully added to keep the tracking document consistent with the current implementation.

## Root Cause
N/A (Documentation sync and verification). Features were developed and recorded in `changes/` but were not migrated over to `docs/TODO.md`.

## Solution
1. Analyzed all design documents (`docs/*.md`) and change logs (`changes/*.md`).
2. Identified conflicts between implementations and design, ensuring they were correctly tracked in `docs/design_conflict.md` and `docs/design_todo.md`.
3. Updated `docs/TODO.md`'s Resolved Items section by adding:
    * **Multi-map RPG Scene Rendering**: Dynamically merge multi-map scenes in RPG mode
    * **Quest Player Restore**: Restore last quest in RPG Mode
4. Removed temporary scripts that caused code review failure and resubmitted the patch cleanly.
