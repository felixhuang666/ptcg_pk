Plan Steps:
1. React Top Toolbar for RPG Mode Editor
   - Add a fixed header overlay (`div` positioned absolute/fixed) at the top of the map when `mode === 'edit'`.
   - The toolbar must be `w-full`, resizing gracefully (Tailwind: `flex w-full overflow-x-auto ...`).
   - Includes: Map Selector (`<select>`), Map Name (`<input>`), "Generate Map" button, "Rename Map" button, "New Map" button.
   - We will need an API endpoint to GET all maps, POST generate map, PUT rename map, POST create new map.

2. Backend FastAPI (`backend/main.py`):
   - Add global memory storage for maps `maps_store = { "main_200": { ... } }` and fallbacks for when Supabase is missing.
   - Since Supabase tables might need a migration, I will add `scripts/migrate_{date_time_str}_xxxx.py` that alters `maps` table to add `name` if not present. Currently `maps` table has `id` and `map_data`. I'll add `name`.
   - Implement Perlin noise map generation in `POST /api/map/generate`:
     - width, height = 200, 200
     - Use `noise.pnoise2` with octaves=4, scale=20.0 to generate a coherent 200x200 array of tile IDs.
     - Thresholds:
       `< -0.1` -> Water (48)
       `-0.1 to 0.1` -> Grass (2) or Grass Bright A (0)
       `> 0.1` -> Mountain/Rock (94) or Dirt (101).
       Let's use `cute_tileset.png`.
     - Save to in_memory maps and (if available) Supabase.

3. Frontend Phaser Tilemap (`src/components/RpgMode.tsx`):
   - Preload `cute_tileset.png`.
   - Change `this.make.tilemap` tileset creation to:
     `this.tileset = this.tilemap.addTilesetImage('cute_tileset', 'tileset_image')!`
   - Update `processImage` logic or `preload` to load `cute_tileset.png` as `tileset_image`.
   - Ensure the tile map array from backend is fed into `make.tilemap({ data: data2D, tileWidth: 32, tileHeight: 32 })`.
   - Update `currentTileType` bindings for keys 1, 2, 3 to use the actual new tile IDs (e.g. 2 for Grass, 48 for Water, 94 for Mountain).

4. Map Selection & Switch:
   - React UI allows selecting map. When changed, fetches new map from `/api/map?id=XXX`.
   - We need to handle this in `RpgMode.tsx` effectively, maybe exposing a `fetchAndRenderMap(mapId)` to `window` or passing via ref/store.

5. Pre Commit & Submission
